---
title: Try the Beatbox Club - Beats for Music and Video
subs:
  - id: loops
    name: Loop Box
    price: $5/mo
    desc: Inside you'll find ~1 min loopable beats in 320kbps MP3 format. Best for IG, Youtube, ads &amp; artist demos.
    stripe: 5000
    paypal: WNTAUEZ2UZMJ6
  - id: tracks
    name: Track Box
    price: $20/mo
    desc: Inside you'll find full length beats in 320kbps MP3 format. Best for music artists &amp; filmmakers.
    stripe: 20000
    paypal: KMEXEAVXZERL2
  # - id: stems
  #   name: Stem Box
  #   price: $50/mo
  #   desc: Inside you'll find full length beats with their stems in MP3 & WAVformat. Best for anyone who wants to customize shit.
  #   stripe: 50000
---
section.block.block-section
  .container
    .row
      .col-12
        h1
          | Join the Beatbox Club today to get access to hiphop beats for your music, videos, ads or life.
section.block.block-section
  .container
    .row
      .col-12
        .row
          - current_page.data.subs.each do |subs|
            .col-12.col-lg-6
              #plan.card-plan data-amount="#{subs.stripe}" data-name="#{subs.name}" data-desc="#{subs.desc}" data-id="#{subs.id}"
                .card-plan-inner.card-plan-content
                  h3 = subs.name
                  p = subs.price
                  p.content-small = subs.desc
                .card-plan-inner.card-plan-cta
                  / button.subs-submit.form-submit.btn.btn-primary value="Subscribe" name="subscribe" id="#{subs.id}" Subscribe
                  form action="https://www.paypal.com/cgi-bin/webscr" method="post" target="_top"
                    input name="cmd" type="hidden" value="_s-xclick"
                    input name="hosted_button_id" type="hidden" value="#{subs.paypal}"
                    input.subs-submit.form-submit.btn.btn-primary type="submit" border="0" name="submit" value="Subscribe"
                    img alt="" border="0" height="1" src="https://www.paypalobjects.com/en_US/i/scr/pixel.gif" width="1"
